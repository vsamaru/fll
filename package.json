{
  "name": "flareact-template",
  "version": "1.0.0",

  "main": "index.js",
  "private": true,
  "scripts": {
      "g": "git add . && git commit -m 'Initial commit' && git branch -M master && git push -f",
    "dev": "flareact dev",
    "build": "flareact build",
    "deploy": "flareact publish"
  },
  "dependencies": {
    "flareact": "^1.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  }
}
